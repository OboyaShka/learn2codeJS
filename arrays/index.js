const people = [
    {name: 'Денис', age: 21, budget: 1000},
    {name: 'Ефим', age: 22, budget: 43300},
    {name: 'Влад', age: 22, budget: 4000},
    {name: 'Данил', age: 22, budget: 1230},
    {name: 'Семён', age: 19, budget: 40100},
    {name: 'Максим', age: 23, budget: 3000},
]

// for (let i = 0; i <people.length; i++) {
//     console.log(people[i])
// }
//
// for (let person of people){
//     console.log(person)
// }

// people.forEach(function (person, index, ppArr) {
//     console.log(person)
// })

// people.forEach(person => console.log(person))

// const peopleNames = people.map(person => person.name)

// const peopleNames = people.filter(person => person.age === 21)

// const amount = people.reduce((total, person) =>  total + person.budget, 0)

// const denis = people.find(person => person.name === 'Денис')

// const denisIndex = people.findIndex(person => person.name === 'Денис')

//====================================